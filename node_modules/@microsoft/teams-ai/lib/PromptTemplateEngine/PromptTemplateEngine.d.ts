/**
 * @module teams-ai
 */
/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
import { TurnContext } from 'botbuilder';
import { PromptManager } from '../Prompts';
import { TurnState } from '../TurnState';
import { Block } from './Block';
/**
 * @private
 */
export declare class PromptTemplateEngine<TState extends TurnState> {
    private readonly _promptManager;
    constructor(promptManager: PromptManager<TState>);
    extractBlocks(templateText?: string, validate?: boolean): Block[];
    render(context: TurnContext, state: TState, textOrBlocks: string | Block[]): Promise<string>;
    renderVariables(context: TurnContext, state: TState, blocks: Block[]): Block[];
    renderCode(context: TurnContext, state: TState, blocks: Block[]): Promise<Block[]>;
    private readonly starter;
    private readonly ender;
    private tokenizeInternal;
    private validateBlocksSyntax;
}
//# sourceMappingURL=PromptTemplateEngine.d.ts.map